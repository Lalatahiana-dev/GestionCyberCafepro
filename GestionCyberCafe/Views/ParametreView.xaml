<UserControl x:Class="Gestion_CyberCafe.Views.Parametre.ParametreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F9FAFB">

    <Grid Margin="20">

        <Border Background="White"
                CornerRadius="12"
                Padding="20"
                BorderBrush="#E5E7EB"
                BorderThickness="1">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- ================= TITLE ================= -->
                <TextBlock Grid.Row="0"
                           Text="Paramètres"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="#111827"
                           Margin="0,0,0,15"/>

                <!-- ================= FULL WIDTH TAB MENU ================= -->
                <Border Grid.Row="1"
                        Background="#F3F4F6"
                        CornerRadius="10"
                        Padding="5"
                        Margin="0,0,0,15">

                    <UniformGrid Columns="4">

                        <!-- GENERAL -->
                        <Button x:Name="btnGeneral"
                                Content="🏢 Général"
                                Height="40"
                                Background="White"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="BtnGeneral_Click">

                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border"
                                                        Background="{TemplateBinding Background}"
                                                        CornerRadius="8"
                                                        Padding="8">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                </Border>

                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border"
                                                                Property="Background"
                                                                Value="#E5E7EB"/>
                                                    </Trigger>

                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="border"
                                                                Property="Background"
                                                                Value="#D1D5DB"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>

                        </Button>

                        <!-- TARIFS -->
                        <Button x:Name="btnTarifs"
                                Content="💰 Tarifs"
                                Height="40"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="BtnTarifs_Click"/>

                        <!-- USERS -->
                        <Button x:Name="btnUsers"
                                Content="👤 Utilisateurs"
                                Height="40"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="BtnUsers_Click"/>

                        <!-- POSTES -->
                        <Button x:Name="btnPostes"
                                Content="🖥 Postes"
                                Height="40"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="BtnPostes_Click"/>

                    </UniformGrid>

                </Border>

                <!-- ================= CONTENT ================= -->
                <Border Grid.Row="2"
                        Background="#FFFFFF"
                        CornerRadius="10"
                        Padding="15"
                        BorderBrush="#E5E7EB"
                        BorderThickness="1">

                    <ContentControl x:Name="ParametreContent"/>

                </Border>

            </Grid>

        </Border>

    </Grid>

</UserControl>